services:
  - type: web
    name: stratagem-ai-test
    runtime: python
    region: oregon
    plan: free
    branch: main
    buildCommand: pip install -r requirements.minimal.txt
    startCommand: uvicorn app.main_minimal:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
